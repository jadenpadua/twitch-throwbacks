import React from 'react';
import { Button } from 'antd';

import './style.less';

const StopThrowback = props => {
  return (
    <div className = "button">
      <Button>Stop throwback</Button>
        </div>
  );
};

export default StopThrowback;
